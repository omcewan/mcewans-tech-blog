<div class='row'>
  <form class='col-3'>
    <h2>New Post</h2>
    <div class='mb-3'>
      <label for='postTitle' class='form-label'>Title</label>
      <input type='input' class='form-control' id='post-title' />
    </div>
    <div class='mb-3'>
      <label for='postContents' class='form-label'> Post Contents</label>
      <textarea class='form-control' id='post-contents' rows='3'></textarea>
    </div>
    <button type='submit' class='btn btn-primary post-btn'>Add Post</button>
  </form>
  <ol class='col'>
    <h2>Your Posts</h2>
    <li>
      {{#each posts}}
        <div class='card'>
          <div class='card-header'>
            <p class='title'>Title: {{title}}</p>
          </div>
          <div class='card-body'>
            <p class='posted-by'>Posted By:
              {{user.username}}
              on
              {{createdAt}}</p>
            <p class='card-text'>{{post_contents}}</p>
          </div>
        </div>
      {{/each}}
    </li>
  </ol>
</div>
<script src='/javascript/add-post.js'></script>